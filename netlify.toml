[build]
  command = "npm ci --prefer-online && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback - serve index.html for all other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
